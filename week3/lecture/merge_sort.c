#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int numbers[] = {20, 500, 10, 5, 100, 1, 50};

    merge_sort(numbers)

}

int new[] merge_sort(int sort[])
{
    int n = sizeof(sort);
    int sort1[] = sort[0:]
    int n2 = sizeof(sort2);


}
